# DayZLogParser

Парсер логов DayZ с графическим интерфейсом на JavaFX.

## 🚀 Обзор

Этот проект парсит логи DayZ и визуализирует результаты.  
Логи могут быть размещены в папке `logs/` рядом с исполняемым JAR-файлом.

## 📦 Сборка

Проект использует **Maven**.
Собрать проект:
```sh
mvn clean package
```
Появятся джарники в /target/jars. Подготовить и собрать .exe:
```
mvn jlink:jlink
mvn jpackage:jpackage
```
## 📦 Структура репозитория
```
/
├── src/                # Исходники
├── logs/               # Логи, которые парсит приложение. Приложение ожидает такую папку в своей директории
├── .gitignore
├── pom.xml
└── README.md
```

## Работа с логами
При запуске приложение ожидает, что у него рядом с собой будет папка:
```
logs/
```
и внутри неё — файлы логов DayZ.
Либо ты можешь указать путь к папке с логами после запуска приложения.
